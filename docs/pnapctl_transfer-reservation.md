## pnapctl transfer-reservation

Transfer a reservation from one point to another(??)

### Synopsis

REPLACE

```
pnapctl transfer-reservation [flags]
```

### Options

```
  -h, --help   help for transfer-reservation
```

### Options inherited from parent commands

```
      --config string   config file defaults to the environment variable "PNAPCTL_HOME" or "pnap.yaml" in the home directory.
  -v, --verbose         change log level from Warn (default) to Debug.
```

### SEE ALSO

* [pnapctl](pnapctl.md)	 - pnapctl creates new and manages existing bare metal servers.
* [pnapctl transfer-reservation server](pnapctl_transfer-reservation_server.md)	 - Transfer reservation of server elsewhere.

